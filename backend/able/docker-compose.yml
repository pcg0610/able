services:
  able-server:
    image: able-server
    container_name: able-server
    volumes:
      - /usr/local/able:/app/able
    ports: 
      - "8000:8000"
    networks:
    - able-network


networks:
  able-network:
    name: able-network 
    external: true