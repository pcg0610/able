services:
  able-server:
    image: able-server
    container_name: able-server
    volumes:
      - /usr/local/able:/app/data
    ports: 
      - "8000:8000"
    networks:
    - able-network


networks:
  able-network:
    driver: bridge